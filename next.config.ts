/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
      domains: ['cdn.bimmertoday.de', 'www.autoreifenfinder.at'], // Füge hier die neue Domain hinzu
  },
}

module.exports = nextConfig;
